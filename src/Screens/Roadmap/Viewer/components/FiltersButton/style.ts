import { StyleSheet } from "react-native";

const styleFiltersButton = StyleSheet.create({
  buttonFilter: {
    borderRadius: 8,
    borderColor: "#549DB4",
    justifyContent: "center",
    alignItems: "center",
    height: 28,
  },
});

export default styleFiltersButton;
